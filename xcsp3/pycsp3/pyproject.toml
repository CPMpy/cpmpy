[tool.poetry]
name = "pycsp3"
version = "0.1.0"
description = ""
authors = ["None"]
readme = "README.md"
package-mode = true
packages = [{include = "pycsp3"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
